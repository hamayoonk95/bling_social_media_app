<div class="post">
    <!-- Author Info -->
    <div class="post-header">
        <!-- Add a default profile pic or use Gravatar -->
        <h3><%= post.author.username %></h3>
        <span class="post-date"
            ><%= new Date(post.createdAt).toLocaleString() %></span
        >
        <!-- The above will format the date and time nicely -->
    </div>

    <!-- Post Content -->
    <div class="post-content">
        <p><%= post.content %></p>
    </div>

    <!-- Post Interactions (Like, Comment, etc.) -->
    <div class="post-interactions">
        <span class="likes">
            <%= post.likes.length %> Likes
            <!-- You can enhance this by making it clickable to show who liked the post -->
        </span>
        <span class="comments">
            <%= post.comments.length %> Comments
            <!-- Similarly, clickable to show comments -->
        </span>

        <% if(user && user.id == post.author._id) { %>
        <span class="delete-action">
            <form action="/posts/<%= post._id %>?_method=DELETE" method="POST">
                <button type="submit" class="delete-btn">Delete</button>
            </form>
        </span>
        <% } %>
    </div>
</div>
