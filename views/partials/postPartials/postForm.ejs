<% if(user) { %>
<!-- Ensure user is logged in to show the form -->
<form
    class="form post-form"
    action="/posts/new"
    method="POST"
    class="post-form"
>
    <textarea
        name="content"
        placeholder="What's happening?"
        class="input post-input"
        required
    ></textarea>
    <button type="submit" class="btn post-btn">Post</button>
</form>
<% } else { %>
<p>
    You need to be logged in to make a post.
    <a href="/users/login" class="login-link">Login here</a>
</p>
<% } %>
